#include <iostream>
#include <cstdio>
#include <cstdlib>
using namespace std;

void GenerateData(const int num_insert, const int num_query){
    freopen("poj.txt","r",stdin);
    freopen("data.txt","w",stdout);
    int total = num_insert + num_query;
}

int main(){
    int num_insert,num_query;
    cin>>num_insert>>num_query;
    GenerateData(num_insert,num_query);
    return 0;
}